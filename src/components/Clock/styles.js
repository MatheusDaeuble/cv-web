import styled from 'styled-components';
import { colors } from '~/styles';

export const Time = styled.div`
  position: absolute;
  z-index: 1;
  top: 7px;
  font-size: 11px;
  font-weight: bold;
  color: ${colors.black};
  right: 22px;
`;
