import styled from 'styled-components';
import { colors } from '~/styles';

export const Footer = styled.div`
  text-align: center;
  font-size: 14px;
  font-weight: bold;
  padding-top: 20px;
  color: ${colors.darker};
`;
